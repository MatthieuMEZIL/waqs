Download NuGet.exe(www.nuget.org/nuget.exe)
Edit the system environment variables, add NuGet.exe folder path in environment path ("<existing path>;<nuget folder path>)
Start Developer Command Prompt for VS
	powershell
	.\build.ps1
